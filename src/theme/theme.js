import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#2196F3",
    secondary: "#03DAC6",
    tertiary: "#FFC107",
    error: "#B00020",
    background: "#F5F5F5",
    surface: "#FFFFFF",
    onSurface: "#000000",
    onBackground: "#000000",
  },
};

export default theme;
